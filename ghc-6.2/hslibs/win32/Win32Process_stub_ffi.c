/* Auto generated GreenCard 2 code for FFI */
#include <windows.h>
#include "win32debug.h"
#include "Win32Process_stub_ffi.h"
DWORD prim_iNFINITE()
{ DWORD res1;
  do {res1=INFINITE;
      
      return((DWORD)(res1));} while(0);
}
void prim_sleep(DWORD arg1)
{ do {Sleep(arg1);
      ;} while(0);
}
